import { userRegistrationModel } from "../models/userRegistrationModel"

export const mutationsResolvers = {
  registerUser: (
    parent: any,
    args: {userDetails: userRegistrationModel},
    context: any,
    info: any
  ) => {

  }
}