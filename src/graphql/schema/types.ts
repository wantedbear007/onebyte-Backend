const types = `#graphql
  type User {
    userId: String,
    name: String,
    email: String,
    username: String,
    lastLogin: String,
    bio: String
    pictureURL: String
  }
  `;

export default types;
